// lib/SystemState/SystemState.cpp
#include "SystemState.h"

// All fields are initialised in the header and no module needs behaviour from
// SystemState itself. This file exists so the module has the same shape as
// every other one, and so there is somewhere obvious to put derived getters
// if the node ever grows them.
